# Codeception Test Suite Configuration
#
# Suite for API tests.

actor: ApiTester
modules:
  enabled:
    - Asserts
    - Helper\Api
    - \Helper\Integration
    - PhpBrowser:
        url: 'http://%APP_IP%'
    - REST:
        depends: PhpBrowser
#        depends: Phalcon
